# Web framework + server
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4

# Speech-to-text (word timestamps)
faster-whisper==1.1.1
# faster-whisper 1.1.1 imports `requests` directly, but recent huggingface_hub
# no longer pulls it in — declare it explicitly.
requests==2.34.2

# Audio decoding + numerics (imported directly in app/; also faster-whisper deps,
# pinned here so the versions we build against are explicit)
av==17.1.0
numpy==2.4.6

# Acoustic analysis (pitch, intensity, jitter/shimmer/HNR)
praat-parselmouth==0.4.6

# Coaching + tailored topics (Anthropic Messages API)
anthropic==0.69.0
